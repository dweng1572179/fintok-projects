# Vendored into runtime/python/{arch}/lib/python3.12/site-packages (mac) and
# runtime/python/Lib/site-packages (win) by build.sh's pyinstall(). Top-level
# only — pip resolves + vendors transitive deps (cryptography, lxml runtime
# deps, pypdfium2, xlsxwriter, etc.) automatically from these pins.
#
# staticmap, pillow      -> kit skills/cre-maps, skills/property-photos (kit's own documented deps)
# python-pptx, python-docx, openpyxl -> doc-from-template skill (agent-authored editable decks/models)
# pypdf, pdfplumber       -> anthropic-skills/skills/pdf (pypdf*, pdfplumber imports)
# defusedxml, lxml        -> anthropic-skills/skills/{pptx,docx}/scripts/office/** (OOXML pack/unpack/validate)
# pdf2image                -> anthropic-skills/skills/pdf/scripts/convert_pdf_to_images.py
#   NOTE: pdf2image needs the system `poppler` binary (like the office skills need
#   system LibreOffice/`soffice`) — neither is bundled by build.sh; same class of
#   pre-existing external-binary assumption baked into the vendored skills, out of
#   scope for this bundle per the Task 8 brief.
staticmap==0.5.7
pillow==11.1.0
python-pptx==1.0.2
python-docx==1.1.2
pypdf==5.1.0
pdfplumber==0.11.4
openpyxl==3.1.5
defusedxml==0.7.1
lxml==5.3.0
pdf2image==1.17.0
